# Проект: Video Admin (HLS-транскодер)

## Что это

Учебный pet-проект второго этапа roadmap.html ("Backend-минимум"), но вместо
каталога фильмов по TMDB — веб-админка с загрузкой видео и HLS-стримингом.
Цель та же, что и в roadmap: получить рабочий backend-фундамент (FastAPI +
Postgres + авторизация + Docker) и параллельно закрыть более сложную и
практичную задачу — приём больших файлов и их обработку в фоне.

Роль Claude в этом проекте: **не писать бизнес-логику за меня**, если явно не
попросил. Основная работа — объяснять, как что должно работать, задавать
наводящие вопросы, ревьюить написанный мной код, указывать на ошибки и best
practices. Я учусь, а не делегирую.

Исключение: **тесты Claude пишет сам, по умолчанию, без отдельной просьбы**
на каждом этапе — как только бизнес-логика этапа готова и обсуждена. Модели,
роуты, сервисы, репозитории, архитектурные решения — по-прежнему пишу я сам.

## Мои вводные

- Закончил этап 1 (Python-синтаксис) из roadmap.html. Уверенно читаю/пишу
  базовый Python, но ещё не проходил: генераторы/итераторы, functional
  (map/filter/reduce, декораторы, type hints), asyncio, pytest, стандартную
  библиотеку глубоко. Это придётся подхватывать по ходу проекта — не считать,
  что я уже всё это знаю.
- Backend делаю впервые. Docker, PostgreSQL, Redis, nginx — впервые.
- 5 лет опыта в frontend (SmartTV, браузер, мобильные) — SPA, стриминговый
  UI, работа с состоянием на клиенте объяснять не нужно, это сильная сторона.

## Архитектурные решения (зафиксированы, не пересматривать без явного повода)

| Вопрос | Решение | Почему |
|---|---|---|
| Транскодирование | Фоновая очередь: Celery или RQ + Redis как брокер | Синхронно/BackgroundTasks не переживёт долгую обработку 10ГБ файла и не масштабируется на несколько воркеров |
| Загрузка файла | Chunked/resumable upload | Единственный реалистичный способ надёжно принять файл в единицы–десятки ГБ через HTTP |
| Хранилище видео/HLS | MinIO (S3-совместимое, локально в Docker) | Ближе к тому, как это делают в проде (объектное хранилище, не локальный диск) |
| Frontend | Отдельный SPA (React или Vue) поверх API | У меня уже есть сильный frontend-опыт — делаю по-взрослому, с разделением backend/frontend как в реальных проектах |
| Пользователи | Полноценная таблица users, регистрация, роли (admin/viewer) | Не "один admin из .env" — хочу практику именно с авторизацией как доменной задачей |
| БД | PostgreSQL + SQLAlchemy (ORM) + Alembic (миграции) | Из roadmap.html, плюс миграции — то, что реально используется в проде |
| Веб-сервер | nginx перед FastAPI, отдаёт HLS-сегменты и, возможно, статику SPA | Учебная цель — понять зачем нужен reverse proxy и как раздавать медиа с range-requests |
| Python deps | uv (не poetry, не голый pip+venv) | Быстрее, единый инструмент для venv+pip+версий Python, де-факто стандарт в новых проектах |
| Форматирование/линт | Только ruff (не black+ruff отдельно) | У ruff свой форматтер, совместимый с black; один инструмент вместо двух, от той же команды (Astral), что и uv |

## Этапы

Работаем строго по порядку. Не начинать следующий этап, пока предыдущий не
закрыт (код работает, покрыт тестами хотя бы по минимуму, задача понятна не
только "по ощущениям", а я могу объяснить, как это работает).

### Этап 0 — Настройка рабочего окружения (закрыт)

Цель: с нуля поднять окружение, в котором дальше будет вестись вся разработка.
Ничего из бизнес-логики ещё не пишем.

- [x] Git-репозиторий: `git init`
- [x] `.gitignore` (Python, Docker, IDE, .env)
- [x] Структура каталогов проекта: `backend/`, `frontend/` (пока пустой),
      `docker/` или docker-compose в корне
- [x] Python: uv, зафиксировать версию Python, pyproject.toml
- [x] Установить и разобраться с Docker Desktop (Windows) — что такое образ,
      контейнер, volume, network, отличие от простого exe/процесса
- [x] docker-compose.yml с сервисами: `postgres`, `redis`, `minio` (пока без
      своего приложения) — поднять, убедиться что все три живы
      (`docker compose ps`, логи, подключение клиентом/CLI к каждому)
- [x] Минимальный FastAPI-скелет: один эндпоинт `/health`, который отвечает
      200 OK — без БД, без авторизации. Приложение живёт вне Docker,
      инфраструктура (postgres/redis/minio) — внутри
- [x] Первый тест: pytest + httpx/TestClient на этот `/health` — чтобы с
      первого дня тестовый контур был настроен, а не откладывался
- [x] Определиться и настроить: ruff (форматирование и линт, замена
      black+ruff одним инструментом) — VSCode настроен на formatOnSave

**Итог этапа:** `docker compose up` поднимает postgres+redis+minio, FastAPI
отвечает на /health, есть один проходящий тест, есть .gitignore и структура
каталогов. Ничего "бизнесового" ещё нет — и это нормально.

### Этап 1 — Авторизация (закрыт)

Цель: рабочая регистрация/логин, без видео вообще.

- [x] Модель `User` в Postgres (SQLAlchemy), миграция через Alembic
- [x] Хеширование паролей (bcrypt/passlib)
- [x] JWT-токены (access + refresh, хранится хэш refresh-токена в БД)
- [x] Роуты: регистрация, логин, refresh, logout, "текущий пользователь" (/me)
- [x] Роли (admin/viewer) и зависимость `require_role` для проверки роли
- [x] Тесты: регистрация, логин (верный/неверный/несуществующий), /me
      (валидный/просроченный/мусорный/отсутствующий токен), refresh (успех,
      без токена, невалидный токен, повторное использование старого
      refresh-токена после ротации), logout (отзыв refresh-токена, no-op без
      токена), `require_role` (разрешает/запрещает по роли). 22 теста,
      `backend/tests/test_auth.py` + `backend/tests/conftest.py`.

**Шаги/находки:**
- Миграции Alembic (`create_users_table`, `add_refresh_token_table`) были
  сделаны раньше и не потребовали правок для этапа.
- При написании тестов всплыл баг в фикстуре `unique_email` (conftest.py):
  удаление пользователя падало с `ForeignKeyViolationError`, если у него уже
  был выпущен `refresh_token` — фикстура не чистила связанные токены перед
  удалением user. Починено: сначала `DELETE FROM refresh_token WHERE
  user_id = ...`, потом удаление user.
- Найдено (не починено, только смягчён тест): access-токен не гарантированно
  уникален при двух `login`/`refresh` в пределах одной секунды — `exp`
  считается с точностью до секунды, а payload больше ничего не меняет.
  Обсудить отдельно, стоит ли добавлять `iat`/`jti` в payload.

### Этап 2 — Простой CRUD для видео (ещё без файлов)

Цель: научиться доменной модели и связке с пользователем, прежде чем
усложнять загрузкой файлов.

- Модель `Video` (метаданные: название, описание, статус обработки, owner_id)
  — сделано на Этапе 3 попутно, есть `create`
- CRUD роуты поверх этой модели (list/get/delete — остались после того, как
  create и upload уже реализованы на Этапе 3), без реальной загрузки файла —
  просто чтобы обкатать паттерн: роут → сервис → репозиторий → БД, и тесты
  к нему
- Здесь же — обсудить и выбрать структуру проекта (слои, где что живёт)

**Порядок изменён сознательно:** пользователь начал писать `start_upload`
раньше, чем list/get/delete — решили не откатывать сделанное, а
поменять местами Этап 2 и Этап 3. Сначала полностью закрываем chunked
upload (Этап 3, ТЕКУЩИЙ), затем возвращаемся и добираем оставшийся CRUD
(list/get/delete) как Этап 2.

### Этап 3 — Загрузка большого файла (chunked upload) (ТЕКУЩИЙ)

Цель: научиться принимать файл в единицы ГБ по частям.

- [x] Модель `Video` + роут `create` (метаданные без файла)
- [x] Модель `VideoUpload` (миграция `create_video_uploads_table`), роут
      `POST /videos/{id}/upload` — регистрирует начало загрузки
      (`total_size`, `storage_path`), переводит `Video.status` в `UPLOADING`
- Протокол chunked upload: как клиент режет файл, как сервер собирает
  (обсудить конкретную схему до кода)
- Хранение чанков и финальная сборка
- Загрузка собранного файла в MinIO
- Тесты: загрузка маленького файла по частям, обработка обрыва/повтора чанка

### Этап 4 — Транскодирование в HLS через очередь

Цель: собранный файл асинхронно превращается в HLS через ffmpeg.

- Установка/использование ffmpeg из Python-кода
- Redis + Celery/RQ: воркер, очередь задач, статусы задачи
- Задача: взять файл из MinIO → ffmpeg → сегменты .ts + .m3u8 → загрузить
  обратно в MinIO
- Обновление статуса `Video` (pending/processing/ready/failed)
- Тесты: тестируем логику постановки задачи и обновления статуса отдельно от
  реального ffmpeg (мокаем внешний процесс) + отдельный ручной/интеграционный
  прогон с реальным маленьким видео

### Этап 5 — Раздача HLS и плеер

Цель: посмотреть загруженное видео в браузере через HLS.

- nginx: конфиг для раздачи сегментов (или проксирования к MinIO), range
  requests, CORS если нужно
- SPA: страница логина, страница загрузки (с прогрессом), список видео,
  плеер на hls.js
- Ручная проверка полного пути: логин → загрузка mp4 → дождаться обработки →
  посмотреть в плеере

### Этап 6 — Докеризация всего приложения + докрутка тестов

Цель: весь стек поднимается одной командой, тесты покрывают весь путь.

- Dockerfile для backend и для worker'а (или общий образ, разные команды)
- docker-compose: app, worker, postgres, redis, minio, nginx — всё вместе
- Обзор покрытия тестами: где дыры, добавить интеграционные тесты (реальный
  Postgres/Redis в тестах через testcontainers или docker-compose.test.yml)
- README с инструкцией запуска

## Как мы работаем вместе (правила для Claude)

- **Не писать код за пользователя**, если явно не попросил. Объяснять
  концепции, архитектуру, тыкать в проблемы в его коде, задавать наводящие
  вопросы. Дефолт — обсуждение и ревью, не реализация.
- Если пользователь просит написать код явно — это разовое исключение для
  конкретной задачи, не смена режима работы на постоянку.
- **Не выполнять команды (миграции, установка пакетов, `ruff --fix`, запуск
  тестов, git-операции и т.п.) без личной просьбы пользователя.** Даже когда
  задача явно требует такой команды (например, тесты не запустить без
  `pytest`) — сначала спросить или явно предложить и дождаться подтверждения,
  не делать по умолчанию "раз уж пишу тесты, заодно и прогоню". Исключение —
  команды, которые сам пользователь попросил выполнить в рамках задачи.
- При ревью — не просто "ок, работает", а объяснять **почему** так лучше
  или хуже, какие есть альтернативы, что будет проблемой в проде.
- Не забегать вперёд по этапам — если пользователь спрашивает про этап 4,
  пока не сделан этап 1, не стесняться напомнить про порядок и спросить,
  осознанно ли он перескакивает.
- Не считать, что пользователь уже знает то, что помечено как "не пройдено"
  в разделе "Мои вводные" — базовые вещи Python вроде декораторов, asyncio,
  pytest нужно объяснять с нуля при первой встрече в коде, а не предполагать
  знание.
- Технологии (Docker, Postgres, Redis, nginx, MinIO, Celery/RQ) — пользователь
  видит их впервые. Не считать очевидным ни один термин (образ vs контейнер,
  брокер vs воркер, миграция и т.д.) — объяснять при первом появлении.
- Обновлять чекбоксы и статус этапа в этом файле по ходу работы, когда
  пользователь подтверждает, что пункт сделан.
- Тесты к бизнес-логике этапа пишет Claude сам, как только логика готова и
  обсуждена — не дожидаясь отдельной просьбы каждый раз. Если по ходу
  тестирования всплывает баг в коде пользователя — не чинить его молча,
  явно указать на находку и согласовать фикс.
- По ходу разработки каждого этапа — как только сделан значимый шаг
  (реализована модель, роут, миграция, найден и исправлен баг, принято
  архитектурное решение) — фиксировать это в разделе этапа короткой записью:
  что сделано и почему, а не только финальным списком чекбоксов в конце.
  Это не журнал каждой мелкой правки, а трасса ключевых шагов, по которой
  потом можно восстановить, как и почему что-то получилось так, как есть.
